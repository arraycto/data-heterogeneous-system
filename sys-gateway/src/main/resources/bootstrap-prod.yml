spring:
  cloud:
    nacos:
      discovery:
        server-addr: http://10.138.130.1:32925
      config:
        server-addr: http://10.138.130.1:32925
        file-extension: yaml
  redis:
    host: 10.138.130.1 # Redis服务器地址
    database: 0 # Redis数据库索引（默认为0）
    port: 34982 # Redis服务器连接端口
    #    password: deltaqin  # Redis服务器连接密码（默认为空）
    timeout: 5000ms # 连接超时时间（毫秒）
